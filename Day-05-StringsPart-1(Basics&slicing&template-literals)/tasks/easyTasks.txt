🟢 10 EASY TASKS

Create a string and print its length.

Access first and last character of a string.

Print each character using a loop.

Convert string to uppercase (basic).

Convert string to lowercase (basic).

Extract first 5 characters using slice.

Extract last 3 characters using slice.

Check if a string contains a word using includes().

Use template literal to print greeting.

Create multi-line string using template literals.